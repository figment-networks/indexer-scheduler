#!/bin/sh

# This file is for development only and should be used by running `source ./config/development/env` from the project root
export DATABASE_URL=postgres://scheduler:scheduler@localhost:5431/scheduler?sslmode=disable
export AUTH_USER=dev
export AUTH_PASSWORD=dev
export HEALTH_CHECK_INTERVAL=10s
export DESTINATIONS_CONFIG=./config/development/destinations
export SCHEDULES_CONFIG=./config/development/schedules
export ADDRESS=127.0.0.1:8075